# Backend API origin the frontend talks to. The frontend runs as its own app and
# is NOT served by the backend — set this to wherever the Spring Boot API lives.
# Change for production (e.g. https://api.yourhost.com).
VITE_API_BASE=http://localhost:8080
